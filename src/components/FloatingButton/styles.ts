import styled from 'styled-components'

export const BtnFloating = styled.button`
  position: fixed;
  bottom: 24px;
  right: 24px;
  background-color: #1e90ff;
  color: white;
  font-size: 24px;
  padding: 20px 24px;
  border: none;
  border-radius: 50px;
  cursor: pointer;
  box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3);
  z-index: 999;
`
